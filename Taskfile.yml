version: "3"

tasks:
  build:
    desc: Build the app
    cmds:
      - GOFLAGS=-mod=mod go build -o ./bin/app ./internal/main.go

  run:
    desc: Run the app
    cmds:
      - GOFLAGS=-mod=mod go run ./internal/main.go
